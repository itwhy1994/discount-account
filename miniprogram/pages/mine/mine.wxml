<!--pages/mine/mine.wxml-->

<view wx:for="{{strFunctionChooses}}" wx:key="none">
	<view id="{{index+1}}" class="viewListItem" bindtap="bindtapList">
		<text class="textListItem">{{item}}</text>
	</view>
</view>

